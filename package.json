{
  "name": "ghl-stripe-webhook",
  "version": "1.0.0",
  "description": "GHL -> Stripe metered usage webhook",
  "main": "src/server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "stripe": "^16.0.0"
  }
}
